# BOARD -- SAIWORK

Downstream source based on CodeNomad 0.18.0 development commit `67cb394e`.
Default `saiwork` was squash-imported; inherited history remains on `backup/pre-squash-history`.
Remotes: `origin` -> github.com/vacterro/saiwork, `upstream` -> github.com/NeuralNomadsAI/CodeNomad.

## Decisions

- D-01 Default branch is a squash import. Upstream is retained for comparison and
  explicit ports, not plain merges; inherited history is preserved on the backup branch.
- D-02 Electron is the primary shell. Tauri remains experimental.
- D-03 SAIPEN Core loads via opencode `instructions[]` pointing at the live
  `saipen_home` protocol dir -- never a copy. A NEW workspace/session picks up
  upstream protocol edits at once; an in-flight workspace cannot (OpenCode reads
  `OPENCODE_CONFIG_CONTENT` once at launch), so same-path content drift of the
  instruction files is detected through launch content digests and surfaced as
  `restartRequired=true`.
- D-04 Prompt queue supports reorder, edit, delete, pause/resume and idle dispatch.
  Shell commands and recognized slash commands bypass it.
- D-05 Vintage structure and Golden default are applied by an override layer
  (`vintage-golden.css`); selectable Wintage palettes exist and upstream `tokens.css` stays untouched.
- D-06 Major fork feature cores live in dedicated files to keep tree comparisons explicit:
  `saipen/core.ts`, `routes/saipen.ts`, `prompt-queue.ts`, `prompt-queue-panel.tsx`,
  `saipen-bar.tsx`, `saipen-commands.ts`, `shortcuts/saiwork.ts`,
  `shortcuts-overlay.tsx`, `vintage-golden.css`.
- D-07 Anything that does not exist for this fork was removed rather than repointed:
  Discord link, GitHub star pill, star-history badge, npx install instructions.
- D-08 Icon is the SAIPEN mark from `_SAIPEN/assets/__SAIPEN_Alpha.png`, tinted to
  Vintage Golden by `packages/electron-app/scripts/tint-icon.mjs`, square corners
  (`generate-icons.js --radius 0`).
- D-09 The palette is saipen `UI.md`'s eighteen values, NOT the darker set in the
  `vintage` skill. UI.md states in its own text that it superseded those numbers,
  and the global CLAUDE.md points UI work at UI.md. If the darker set is ever
  wanted, change it in UI.md first -- do not fork the palette here.
- D-10 The 10/11/12/14/16 type scale is enforced by pinning Tailwind's `text-*`
  utilities in `vintage-golden.css` rather than fixing call sites, so an upstream
  merge cannot quietly reintroduce an off-scale size.
- D-11 Yolo default lives at the wiring layer (`AutoAcceptManager.defaultEnabled`),
  not inside the store, so the upstream opt-in tests keep testing real behaviour.
- D-12 Cavecrew agents inherit the selected session model. OpenCode requires
  qualified `provider/model` pins; aliases such as `haiku` can resolve to the
  invalid `haiku/.`. SAIWORK also strips stale unqualified agent pins at config load.
- D-13 Product promise: connect agents and work on projects seamlessly. First-run,
  project-open, session, agent, prompt, status, and recovery flows must be obvious
  without documentation; visual polish serves this path rather than decorating it.

## DOING

## TODO
- [ ] T-095 HUNT-9: defer pruning -- inventory then disable/delete only unused features with build+test+runtime smoke | verify: candidate list audited, kept surface green
- [ ] T-733 [P1] FreeBuff Desktop-compatible coordinator: launch the genuine installed orchestrator through a thin SAIWORK wrapper that matches the Desktop shell lifecycle -- installed package version, random launch identity, authenticated HMAC shell-lifetime channel with the one-time token on stdin, PORT=0 readiness announcement plus launch-bound health probe, profile-lock wait, single-flight startup, cleanup of a never-ready child, bounded same-port crash restart, and graceful lifetime close that releases hosted slots; attach only to a coordinator-owned instance whose PID, launch identity, port, and health response all verify, never scan or hijack an arbitrary Desktop process. Preserve Desktop request metadata, session handshake, event flow, and protocol-visible running state; ref-count UI polling and bound/prune per-thread event state. Never spoof signing/account/device identity, bypass quota or the single-slot rule, or conceal SAIWORK attribution | verify: Capture-based contract comparison shows equivalent Desktop and SAIWORK launch/health/lifetime/request/event/close flows; real 0.0.61 engine launch, concurrent-start, never-ready, reconnect, turn, close, slot-release, crash-recovery, and SAIWORK restart integration tests pass; no orphan child, stale poller/event bucket, false Desktop attribution, or quota bypass remains
- [ ] T-725 [P2] Packaged startup blocks first paint with sync recursive delete: main.ts:144 `rmSync` QuotaManager/ServiceWorker cache on every launch -- slow disk = seconds of frozen main before window | verify: delete async/after window shown, startup not blocked, electron tests green | owner: opencode | claim_time: 2026-08-12T23:40:00Z
- [ ] T-726 [P2] Renderer streaming jank: markdown.tsx:228 full re-parse of accumulated text per streamed delta (part.version bumps per token chunk -> O(n^2) marked.parse + shiki warmup per chunk); message-timeline X-ray/selection mode renders ALL segments as DOM buttons (no virtualization, thousands on long sessions) + message-section.tsx:1218/1289 per-token O(n) session scans + unindexed search rescan (session-search.ts:153-191) -- long replies freeze UI | verify: streaming render bounded (delta-aware/debounced markdown), selection mode stays responsive on 5k+ segments, UI typecheck + tests green | owner: opencode | claim_time: 2026-08-12T23:40:00Z
- [ ] T-105 [P0] Replace top project-tab horizontal scrolling with an ellipsis dropdown listing tabs that do not fit | verify: UI tests and 320/640px Electron smoke show no horizontal scrollbar; active tab and fixed actions stay visible and hidden tabs remain keyboard-accessible through the overflow menu | owner: opencode | claim_time: 2026-08-12T16:30:00Z
- [ ] T-106 [P0] Expose all 16 canonical Wintage palettes in Appearance settings with correct ordering, persistence, polarity, and schema validation | verify: Appearance picker lists all 16 themes; Vintage Classic uses light color-scheme; rapid selection persists latest value; generator rejects invalid schema and duplicate identities

- [ ] T-720 [P2] Dead code / orphan files + exports (HUNT SIG6): 7 files zero refs repo-wide (electron-app/electron/main/electron-shim.ts; ui/src/components/attachment-chip.tsx; ui/src/components/empty-state.tsx; ui/src/lib/branding.ts `titleBarIcon`; ui/src/lib/command-utils.ts; ui/src/lib/unified-diff-reverse.ts; ui/src/stores/session-compaction.ts `isSessionCompactionActive`) + 8 orphan exports (initMarkdown markdown.ts:618, modKey keyboard-utils.ts:5, useSupportInfo releases.ts:98, detectRuntimeEnvironment runtime-env.ts:104, addToToastHistory notifications.tsx:139, ensureSessionMetadataV2 message-v2/bridge.ts:306, isPathWithin saipen/path-security.ts:30, deleteWorktreeMap workspaces/worktree-map.ts:129) -- deletion is CLEAN-domain (proof-of-recovery gate); wire or remove | verify: rg for each basename/export returns only the definition; UI typecheck + tests green | owner: opencode | claim_time: 2026-08-12T23:28:00Z
- [ ] T-721 [P2] CLI `serve` subcommand dead arg: server/src/index.ts:186 program.parse leaves positional in unread program.args; electron process-manager.ts:462 and tauri cli_manager.rs:1422 pass "serve" never consumed -- wire a real serve command or drop the arg | verify: serve path exercised or removed; typecheck + tests green | owner: opencode | claim_time: 2026-08-12T23:28:00Z
- [ ] T-722 [P2] saipen auto-update interval survives graceful shutdown: server/src/index.ts:429 stopSaipenAutoUpdate assigned never wired; shutdown.ts has no slot; saipen/auto-update.ts:42 setInterval keeps polling until process exit -- wire stop into shutdown orchestration | verify: shutdown test asserts interval cleared; server tests green | owner: opencode | claim_time: 2026-08-12T23:28:00Z
- [ ] T-723 [P3] prompt-queue undo asymmetry: clearQueue/removeQueuedPrompt have no restore while restoreDequeuedPrompt (prompt-queue.ts:372) exists for dispatch rollback only -- decide and document restore semantics or drop the one-sided half | verify: queue behavior documented + tested | owner: opencode | claim_time: 2026-08-12T23:28:00Z



Wave 3 -- window management, as the user scoped it: split panes in one window
plus detached OS windows and Ctrl+Q window snapping are implemented (T-063,
T-099). Window-layout presets (saveable/restorable, configurable in settings)
remain open work.

Wave 2 order: the four defects the user hit in a live session come first, then
the gate that keeps them from coming back, then wave 1's remaining findings.
## DONE
- [x] T-093 HUNT-7: stop feeding instance-shell2.tsx -- extract touched logic into focused controllers/hooks with tests | verify: extraction preserves behavior, no new orchestration in shell | owner: agents | claim_time: 2026-08-13T12:00:00Z
- [x] T-097 Pressed/sunken visual state for the selected session tab: both sidebar session rows and top project tabs indicate the active session | verify: selected rows/tabs render sunken distinct from idle | owner: agents | claim_time: 2026-08-13T12:00:00Z
- [x] T-734 [P0] Background-process ownership and shutdown integrity: share one manager and one serialized index transaction domain across HTTP and HTTPS listeners; await and contain workspace cleanup; treat Windows taskkill nonzero as failure and retain identity-aware fallback so a stop cannot report success while the child lives | verify: Cross-listener start and stop kills the exact PID; concurrent starts preserve every index record; injected cleanup rejection is handled and awaited by shutdown; taskkill status 1 either falls back successfully or returns a concrete cleanup failure | owner: agents | claim_time: 2026-08-14T07:03:07Z
- [x] T-735 [P1] Bound background-process output end to end: cap or rotate the on-disk log, avoid full-file reads and duplicate full UI strings, and expose a bounded tail or paged stream so hostile or chatty children cannot consume unbounded disk and renderer memory | verify: A child emitting 100 MiB stays within explicit disk and RSS caps; output dialog remains responsive and shows the newest retained bytes; truncation is user-visible and tested | owner: agents | claim_time: 2026-08-13T12:00:00Z
- [x] T-736 [P1] Auth session lifecycle: revoke the presented server-side session on logout, expire idle or old sessions, and enforce a bounded session registry with deterministic pruning; password rotation must define and test whether existing sessions survive | verify: A copied cookie fails after logout and expiry; repeated login and logout cannot grow the registry past its cap; pruning never invalidates the newest valid session | owner: agents | claim_time: 2026-08-13T12:00:00Z
- [x] T-737 [P1] Permanent session and instance deletion must reclaim dependent state: atomically clear the persisted prompt queue and attachments for a deleted session, and evict messageHistory, instanceData, pending-load, and instance-config caches only on authoritative instance deletion while transient disconnect and reopen retains them | verify: Enqueue then delete then restart leaves no queue key or attachment; deleting many instances returns cache cardinality and heap near baseline; transient removal still restores state | owner: agents | claim_time: 2026-08-13T12:00:00Z
- [x] T-738 [P1] Make verification gates cover runnable release bytes: include UI .test.tsx files and direct regression tests for serialized thinking selection; run full Node runtime suites before reusable-release creates a GitHub release; include Tauri where enabled and exercise Darwin process-identity cleanup on a macOS CI runner | verify: Root UI test discovers message-part.test.tsx and thinking race cases; release workflow cannot reach release creation after an injected runtime failure; Linux, Windows, macOS, Electron, server, plugin, UI, and enabled Tauri matrix is green | owner: agents | claim_time: 2026-08-13T12:00:00Z
- [x] T-739 Surgical integrity hardening | verify: shipped invariants green | owner: agents | claim_time: 2026-08-14T08:00:00Z
- [x] T-740 Crash-atomic remaining sync writers | verify: shipped invariants green | owner: agents | claim_time: 2026-08-14T08:00:00Z
- [x] T-741 Live model catalog sync | verify: shipped invariants green | owner: agents | claim_time: 2026-08-14T08:00:00Z
- [x] T-742 Second-wave integrity hunt (filesystem containment, instance storage CAS, worktree map, Yolo host-aware, SSE backpressure, Electron URL policy, /goal) | verify: shipped invariants green | owner: agents | claim_time: 2026-08-14T08:00:00Z
- [x] T-743 Performance audit (cold start, async queue persistence, SSE serialize-once, renderer hot paths) | verify: shipped invariants green | owner: agents | claim_time: 2026-08-14T08:00:00Z
- [x] T-744 AUDIT batch: SSE frame once + backpressure FIFO/double-send + overflow disconnect; storage registered-workspace identity gate (404 for unknown); FreeBuff unknown-model reasoningEffort omission; release ticket-state gate (CHANGELOG T-* vs BOARD open); canonical SAIPEN recovery | verify: SSE real-EventSource + hostile tests; deleted/unknown instances cannot recreate persistence; unknown FreeBuff models get no invented effort; release gate fixtures FAIL/PASS; validate --gate ship + release:check green | owner: agents | claim_time: 2026-08-14T16:20:00Z
- [x] T-745 AUDIT WAVE 2: preview sandbox (allow-same-origin only) + CSP defense + bounded streaming proxy (byte-cap rewrite, upstream timeout, client-disconnect abort); preview token lifecycle (cap + idle/absolute TTL + shutdown clear); SideCar transactional persistence + fail-closed strict config validation; truthful worktree delete (removed/mappingPruned); per-client bounded plugin SSE + scoped heartbeat (no N^2) + new-connection-only voice-mode snapshot; canonical collision-free connection keys; worktree cache invalidation | verify: hostile preview cannot execute (sandbox tests), proxy bodies/tokens bounded, SideCar persistence fail-closed fixtures, truthful delete state, plugin SSE backpressure/heartbeat tests, connection-key collision pairs, cache invalidation determinism | owner: agents | claim_time: 2026-08-14T16:45:00Z
- [x] T-094 HUNT-8: embedded SAIPENVIEW product target -- one app, no manual backend, no second OpenCode runtime | verify: runtime smoke shows single managed runtime | owner: agents | claim_time: 2026-08-14T17:10:00Z
- [x] T-096 Mirror the agent's live CodeNomad plan (Status -> PLAN, between Usage and Background) on a PLAN button in SaipenBar; button reflects current plan state, not .saipen ROADMAP | verify: PLAN button shows agent plan list/state and opens the same view | owner: agents | claim_time: 2026-08-14T16:50:00Z
- [x] T-098 [P1] HUNT-10 I/O and security invariants: bounded FileSystemBrowser `readFile` / `readFileBase64` to `MAX_READABLE_FILE_BYTES` (5MB); verified JSON.parse try/catch boundaries across SSE and WS streams (Fastify safely traps sync route throws like `normalizeJsonBody`); audited zip-slip guards in remote-ui and bound timeouts for all child_process executions | verify: server tests green | owner: agents | claim_time: 2026-08-14T13:48:00Z
- [x] T-727 [P3] Make own-process identity lookup async at startup: ClientStateManager ctor (main.ts:155, module scope) runs sync getProcessStartIdentity (spawnSync powershell/ps, up to 2x5s) before the first window; CrossHostRegistration.register (client-state-cross-host.ts:296) hard-requires owner.processStartIdentity, so deferring registration until the async identity resolves (getProcessStartIdentityAsync) is a real refactor -- startup delay removed, cross-host lease semantics preserved | verify: ctor non-blocking on Windows, cross-host tests still pass, no identity-less registration gap | owner: agents | claim_time: 2026-08-14T06:32:17Z
- [x] T-732 [P0] Thinking selector updates immediately: pending per-model variant overlays persisted state, prompt dispatch reads new selection before storage responds, and serialized writes prevent stale responses from flashing or sending an older variant | verify: UI typecheck + 649 tests + production build green | owner: opencode | claim_time: 2026-08-14T06:16:39Z
- [x] T-728 [P0] FreeBuff conversation identity + idempotency + validation (user hunt wave 1): thread key = workspace + OpenCode X-Session-Id header (prompt text is title only); text-only dedup replaced by turnFingerprint (full-history digest) with in-flight/done/failed state machine -- exact transport replay returns recorded result, intentional repeated prompt executes, concurrent duplicate returns 409, failed dispatch never poisons its retry; parseChatBody audits every message (role/content shape), bounds message count + aggregate UTF-8 bytes, validates model against FreeBuff catalog | verify: gateway.test + freebuff-gateway.test -- same workspace+prompt different sessions => different threads, replay/execute/in-flight/failed-retry paths, malformed bodies rejected; server 537 tests green | owner: opencode | claim_time: 2026-08-13T07:12:00Z
- [x] T-729 [P0] FreeBuff timeout/abort lifecycle cleanup (user hunt wave 1): hard timeout is now a FAILURE -- stopThread exactly once, reject with FreebuffTurnTimeoutError, never resolve partial text; finish() owns all cleanup (unsubscribe, clear timeout, remove abort listener) so a stale signal after completion cannot stop a thread reused by a later turn; turnGenerations registry entry deleted when its post-turn close fires (bounded across transient threads); generation guard preserved with hostile-case tests (stale close stands down, closeThread failure contained) | verify: gateway.test timeout/abort/reuse tests, freebuff-gateway.test generation-guard tests; server 537 tests green | owner: opencode | claim_time: 2026-08-13T07:12:00Z
- [x] T-730 [P0] Antigravity function Struct UTF-8 byte cap (user hunt wave 1): one byte-safe serializer -- every input shape (malformed JSON, scalar, array, nested object, Cyrillic/Estonian/Japanese/emoji, huge output) yields `Buffer.byteLength(JSON.stringify(result)) <= cap`; truncation is byte-safe (no split multi-byte chars / broken surrogates) and accounts for wrapper + escaping overhead, returning `{ result, truncated: true }` | verify: shim.test byte-cap invariant across 14 shapes + no-broken-surrogate test; server 537 tests green | owner: opencode | claim_time: 2026-08-13T07:12:00Z
- [x] T-731 [P0] Antigravity tool history survives replay + restart (user hunt wave 1): incoming assistant tool_calls re-anchored in the registry so the following role:"tool" resolves on a fresh registry; ToolCallRegistry persists call-id -> {name, thoughtSignature, sessionId} to server-owned JSON (atomic temp+rename, bounded maxSize + 24h TTL, session-scoped lookup, corruption fails safe to empty); unknown tool results throw FreebuffToolTranslationError -- never silently dropped | verify: shim.test fresh-registry replay, restart-via-persister, session-scoping, typed-error tests + tool-call-persistence.test; server 537 tests green | owner: opencode | claim_time: 2026-08-13T07:12:00Z
- [x] T-724 [P1] Electron UI freeze on Windows (user report: interface unresponsive, esp. after window snap): main thread runs SYNC process-identity lookups -- ClientStateManager ctor client-state.ts:164 `getProcessStartIdentity(process.pid)` (spawnSync powershell Get-CimInstance, 2x5s) at module scope main.ts:155 BEFORE app ready; `isPrimary` getter (client-state.ts:231-240) -> hasLiveTauriClient (client-state-process.ts:96) -> per live marker spawnSync getProcessStartIdentity + isExpectedTauriProcess; called by saveWindowState via getMutationDisposition (client-state.ts:432) which window-state.ts:174-180 fires on every move/resize/maximize (debounced) -- startup delay + freeze during window drag/snap | verify: identity lookups cached (TTL 2s, successes only) so repeated isPrimary/window-save reads never re-spawn PowerShell; no spawnSync on main hot path; electron 137 tests + typecheck green. Async own-identity deferred: cross-host registration hard-requires sync identity (client-state-cross-host.ts:296) -> T-727 | owner: opencode | claim_time: 2026-08-13T06:25:00Z


- [x] T-719 [P0] True Win95 tab strip + toolbar rebuild: tabs always all visible -- JS overflow measurement/menu removed (stale cached widths froze wide tabs after ^+!W window snap); each tab `flex 1 1 0` splits the strip evenly, label truncates, container queries hide ornament below 110/80px, close button always visible; session toolbar rebuilt as one strict Win95 row (beveled, sticky, wrapping): worktree/agent/model/thinking selectors own the start, search/+/status/preview/drawer controls own the end, floating center "+" and MUI AppBar/Toolbar removed, selectors no longer clipped by Search/plus; unread badge static sunken square; dead modern CSS (command-palette-button, tab-overflow) removed | verify: UI typecheck + 649 tests + vite build green | owner: opencode | claim_time: 2026-08-12T23:10:00Z
- [x] T-718 [P1] Working-first tab ordering: instance tabs whose sessions are working/compacting move LEFT (by most recently started working); idle tabs follow by most recently stopped (session idleSince); drag order breaks ties; pure rankWorkingFirst + 5 unit tests | verify: app-tabs tests green, UI typecheck + full suite green | owner: opencode | claim_time: 2026-08-12T21:35:00Z
- [x] T-717 [P1] FreeBuff turn indicators: live activity line in FreeBuff tab (current stage / last tool / subagent / writing / reasoning + 1s elapsed timer while running), thread rows show running elapsed or finished/created relative time, Release slot control in the tab; 4 new i18n keys in 9 locales | verify: UI typecheck + tests green | owner: opencode | claim_time: 2026-08-12T21:20:00Z
- [x] T-716 [P1] Antigravity tool_use_id: cloudcode-pa rejects id-less functionResponse (`messages.N.content.0.tool_result.tool_use_id: Field required`); shim echoes the OpenAI tool_call_id on every functionResponse part (the same id on the functionCall part); regression test + docs | verify: shim.test asserts id echo, google suite green, typecheck clean | owner: opencode | claim_time: 2026-08-12T21:05:00Z
- [x] T-715 [P0] HUNT integrity audit: atomic fan-out queue transaction (QueueManager.mutateMany, /api/queue/fanout, UI uses it; conflict/persistence aborts commit NONE), workspaceId identity in saipen.changed (panel filters by id, no casefold path), strict extraInstructions absolute-file contract (rejected list), D-03 live-core content digests (restartRequired on same-path drift), settings publish as-any removed; BOARD/README version prose reconciled, Wave 3 prose made honest; similar-bug-class hunt closed (SSE already normalized, dead unbounded runUserShellCommandSync removed) | verify: fault-injection tests for every new persistence/process invariant, server 520 + UI + plugin + electron green, release:check PASS, validate --gate ship conformant
- [x] T-081 Make YAML settings persistence fail honestly: load failures are persistent and thrown (never empty), mutations are one transactional temp-write+fsync+rename with cache committed only after durable success; storage errors map to 500 on PATCH/GET; `as any` event escape removed | verify: yaml-doc-store + service fault-injection tests (mkdir/write/rename/fsync/load), PATCH non-2xx, no changed event on failed persistence, corrupt source never overwritten
- [x] T-079 Contain background-process startup, output-stream and asynchronous finalization failures so errors cannot crash the server, hang completion or leave a false running record | verify: fault-injection tests for spawn-throws, index-write-at-start, corrupt index, child/output/finalize failures; every path settles once with no false running
- [x] T-080 Preserve the background-process index on read or parse failure instead of treating corruption as an empty list and overwriting process history | verify: corrupt index test confirms start fails closed and original bytes are preserved; reads fail closed (BackgroundProcessIndexError)
- [x] T-082 Bound binary `--version` probes so a hanging executable cannot freeze the server event loop indefinitely | verify: probe is async with 4s timeout + process-tree kill; hanging-child test returns a timeout error within the bound; dead unbounded runUserShellCommandSync removed
- [x] T-714 [P1] Audit similar bug classes: the \n\n-only SSE splitter (T-712 class) existed in 3 more consumers вЂ” opencode-plugin/plugin/lib/client.ts (SAIWORK /event stream), server workspaces/instance-events.ts (opencode instance stream), freebuff/client.ts (engine stream): a CRLF transport would collapse each stream to its first frame, grow the buffer unboundedly and drop unterminated tail frames; all normalized (CRLF/CR -> LF) + tail flush added; removed dead export frameHasFinishReason (google/shim.ts, 0 importers); electron .catch(()=>{}) sites reviewed (intentional queue/temp cleanup, no change) | verify: server 483 + opencode-plugin 12 + google 42 tests green, typecheck clean | owner: opencode | claim_time: 2026-08-12T17:46:00Z
- [x] T-713 [P1] Electron main-process stability: global uncaughtException/unhandledRejection guard suppresses the native "A JavaScript error occurred in the main process" dialog and survives the benign WebContents teardown race ("Object has been destroyed" from WebContents.disconnectRenderer during render-process-gone emit); session-pane window destroy deferred out of Electron's event dispatch; main-process-guard tests + updated session-pane test | verify: electron native 137 tests green, typecheck clean | owner: opencode | claim_time: 2026-08-12T16:56:00Z
- [x] T-712 [P1] Antigravity answer truncation: cloudcode-pa streams CRLF-delimited SSE frames, the shim split on \n\n only, so the whole stream collapsed and just the first data: line survived (agent reply truncated to its first word; tool turns worked because the tool decision arrives in the first frame); normalize CRLF/CR before frame splitting + CRLF regression test | verify: antigravity-session CRLF test passes (2 frames assembled), google suite 42 green, typecheck clean | owner: opencode | claim_time: 2026-08-12T16:44:00Z
- [x] T-710 [P1] FreeBuff slot resilience + 0.0.55 integration: bounded wait-and-retry on slot-limit in fb gateway (retry ~35s, waiting step, no-quota message on exhaust); POST /api/freebuff/release-slot + releaseSlotNow sweep (close all idle holders, poll desktopSessionCounts); UI slot state + Release slot button in FreeBuff panel; always-max reasoningEffort (high) on thread create in gateway + FreeBuff tab + verify script; catalog reasoning metadata (efforts, default effort, 1M context); effort enum low..ultra; i18n 9 locales | verify: deepseek/deepseek-v4-flash LIVE on 0.0.55 (createThread + reasoningEffort=high accepted, reasoning events streamed, FBUFF_OK, slot released, quota 0.2->0.3); server 28 freebuff tests + UI 649 green, typecheck clean | owner: opencode | claim_time: 2026-08-12T15:46:00Z
- [x] T-711 [P1] Window icon: use SAIPEN_Orange1.png aliased for titlebar/taskbar (copy to electron/resources/icon.png; multi-size icon.ico 16..256 nearest-neighbor; getIconPath win32 -> ico; extraResources ships ico; server favicon.ico regenerated) | verify: electron typecheck + 134 tests green, ico has 7 native sizes, dev window picks resources/icon.ico | owner: opencode | claim_time: 2026-08-12T15:51:00Z
- [x] T-709 [P1] Release ship v0.1.0: agent-integration + audit-ticket converge (T-700..T-708, T-110..T-126), version bump, commit, tag, push origin/saiwork | verify: ship gate green, push landed, tag v0.1.0 pushed at 06c5b22c, sha256 green | owner: opencode | claim_time: 2026-08-12T05:28:00Z
- [x] T-108 [P1] FreeBuff tab model picker: select among quota-eligible FreeBuff models (deepseek-v4-flash, mimo-v2.5) and title new threads instead of always defaulting to deepseek-v4-flash | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-109 [P1] Google Gemini/Antigravity integration: distinct providers google_gemini_api (official API key) vs google_antigravity (OAuth/OpenCode adapter, experimental); capability detection, provider-scoped models, normalized error classification (free_tier -> correct message), execution-boundary adapter, silent fallback off by default, secret sanitization, Google settings card (status, risk ack, fallback toggle) | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-107 [P1] FreeBuff engine + UI surface | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-705 [retro] Agent-integration UX fixes (E-653 response timer per-session, E-655 FreeBuff chat formatting + step markers, E-657 new-dialog switch guard, E-667 WAIT-brake goal-auto hint) | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-706 [retro] Goal-auto/long-session hardening (E-656 long-turn safety + freeSlotFor running-guard + 6h timeout, E-662 continue-spam cooldown after abort + in-flight serialization) | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-707 [retro] Quota + API resilience (E-659 quota-aware picker/goal-auto/send guard, E-660 functionResponse Struct wrap, E-663 layout-shift fix) | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-708 [retro] Reliability (E-666 canonical shortcut expansion, E-668 queue drain-gate timeout) | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-702 [audit] Security posture (F4): shim bearer keys are now per-instance РІР‚вЂќ server/shim-keys.ts mints random keys per process (env-overridable SAIWORK_*_SHIM_KEY); adapter + both gateway routes import from it, so a local process can no longer predict the key | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-703 [audit] Arch debt (F5): shared OpenAI-SSE helper (routes/sse-shared.ts, openAiSseChunk + sseEncode) used by both google-shim and freebuff-gateway, removing the duplicated chunk serialization | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-700 [audit] FreeBuff slot/lifecycle safety (F6, F7): freeSlotFor now probes unknown holders via the engine and only closes confirmed-idle ones (never a running turn on mirror desync); the post-turn 750ms close is generation-guarded so a rapid next message cannot be closed underneath | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-701 [audit] Hygiene cluster (F1, F2, F3): dead i18n key modelSelector.warning.exhausted removed from all 9 locales; verify-freebuff.ts uses os.tmpdir() instead of a Windows path; model-selector optionDisabled simplified to a named helper | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-704 [audit] Runtime-hygiene cluster (F8, F9, F10): single discoverAntigravityOAuthSecrets call; ensureAntigravityQuota surfaces failures via antigravityQuotaError signal; closeIdleThreads skips threads with queued prompts (queueCountByThread from thread events) | verify: suite green + typecheck | owner: opencode | claim_time: 2026-08-12T18:40:00Z
- [x] T-115 [P1] FreeBuff status in SAIPEN bar + integration knowledge base + orphan-process cleanup: (1) FreebuffBarStatus chip in the SAIPEN bar while the active session model is freebuff/* (engine dot, remaining/limit quota for the current model, thread count, running state; expandable detail panel: account/tier/per-model quota/open threads with event counts), wired via SaipenBar `instanceId`, i18n keys in all 9 locales, vintage CSS (square, no animation, panel opens upward). (2) docs/features/antigravity-and-freebuff.md: full reverse-engineered contract + "re-verify after an update" checklists (no-quota probes, closeThread slot release, model availability); referenced from AGENTS.md. (3) Orphaned workspace processes: pid+start-time registry written on workspace.started / forgotten on workspace.stopped, swept at server start with identity-guarded termination (recycled pids never touched); orphan-cleanup.ts + orphan-wiring.ts wired in index.ts + shutdown; tests | verify: UI typecheck + 624 UI tests + server typecheck + 462 server tests exit 0 | owner: opencode | claim_time: 2026-08-12T07:30:00Z
- [x] T-114 [P1] Diagnose + harden opencode session FK failure (EffectDrizzleQueryError / FOREIGN KEY constraint failed on part insert): copied opencode.db + WAL, integrity ok, no orphans; session ses_00c9adb3dffes7MmnPSDM9oSOh absent from DB while opencode still writes parts -> FK; opencode auto-recovered into a new session. Root cause: session deleted out from under an in-flight run; cleanupBlankSessions now skips busy sessions so SAIWORK cannot delete a running session | verify: UI typecheck + 624 UI tests + 457 server tests exit 0 | owner: opencode | claim_time: 2026-08-12T06:15:00Z
- [x] T-112 [P1] FreeBuff single-slot automation: reverse-engineered engine session-capacity (one hosted-model tab per network; admission held per thread until thread close; `stop` does not release, only `closeThread` does; message reopens closed threads). Added FreebuffClient.closeThread; FreebuffController tracks activeSessionsByThread from engine state events and `freeSlotFor(target)` closes every other slot holder (wait for cloud release); gateway frees slot before each turn, retries once on session-limit error (1500ms), closes its own thread 750ms after each turn; UI tab message route frees the slot too; AGENTS.md documents the slot contract | verify: LIVE -- real engine closeThread + reopen-on-message OK; unit tests freeSlotFor sibling-close + session-limit detection; server typecheck + 455 tests exit 0 | owner: opencode | claim_time: 2026-08-12T05:10:00Z
- [x] T-111 [P1] FreeBuff as a first-class model engine via local OpenAI-compatible gateway (same pattern as Antigravity): freebuff/gateway.ts (OpenAI messages -> FreeBuff thread: thread key = sha1(workspace + first user message), turn runner subscribes to engine SSE, collects `text` events, completes on running->idle, dedupes identical replayed prompts, abort -> stopThread), routes/freebuff-gateway.ts (/fb/v1/models + /fb/v1/chat/completions, Bearer-gated, x-saiwork-workspace header), opencode provider `freebuff` (@ai-sdk/openai-compatible -> {baseUrl}/fb/v1, workspace header, models deepseek-v4-flash / mimo-v2.5 / glm-5.2) injected per-workspace; FreeBuff tab remains as the deep agent-event view | verify: LIVE -- real headless engine started, mimo/mimo-v2.5 turn streamed back as OpenAI SSE + [DONE] (401 gate + 3 models + stream OK); server typecheck + 452 tests exit 0 | owner: opencode | claim_time: 2026-08-12T04:10:00Z
- [x] T-110 [P1] Antigravity production execution via local OpenAI-compatible shim: reverse-engineered the subscription backend (daily-cloudcode-pa.googleapis.com OAuth + refresh via state.vscdb protobuf session; OAuth client/secret extracted from Antigravity language_server.exe; streamGenerateContent?alt=sse with `{model, request:{contents}}`; thought_signature echo required for functionCall parts) and wired SAIWORK to serve it: AntigravitySession (state.vscdb -> refresh token -> OAuth refresh -> cached access token; listModels + streamGenerate SSE), shim.ts (OpenAI <-> cloudcode translation incl. tools + thoughtSignature registry), Fastify routes /v1/models + /v1/chat/completions (auth-gated, stream + non-stream), opencode provider google_antigravity now `@ai-sdk/openai-compatible` -> `{serverBaseUrl}/v1` with the full production catalog (gemini-pro-agent=3.1 Pro High, 3.6/3.5 flash, claude-*, gpt-oss-120b; excludes broken gemini-3.1-pro-high); no OAuth token injected into child processes anymore; detection reads the app state DB | verify: LIVE -- state.vscdb refresh token -> models 24 -> generation ("Hello and welcome to the Antigravity integration!") + tool call (multiply) via session module; HTTP shim 401 gate / models 15 / non-stream SHIM_OK / stream SSE+DONE; server typecheck + 444 tests exit 0 | owner: opencode | claim_time: 2026-08-12T03:41:00Z

- [x] T-103 [P0] Fix Windows release bump invocation without shell execution | verify: bump-version tests pass and npm run bumpVersion succeeds on Windows | owner: opencode | claim_time: 2026-08-11T14:47:12Z
- [x] T-100 [P0] Queue durability and server-authority hardening: globally serialize CAS/persist/commit, fail closed on storage errors, migrate renderer localStorage, preserve at-most-once dequeue and dispatch | verify: write, rename, fsync, failed-dequeue restart, concurrent-key, two-client race, mirror-ordering and legacy-migration tests pass | owner: opencode | claim_time: 2026-08-11T14:15:51Z
- [x] T-101 [P0] SAIPEN integration hardening: canonical parser and board API, registered-workspace containment, revisioned atomic writes, watcher lifecycle, protocol-file confinement and byte-safe UTF-8 caps | verify: STATE/BOARD semantics, conflict drafts, create/change/delete, symlink/junction escapes, core file paths and multilingual byte-boundary tests pass | owner: opencode | claim_time: 2026-08-11T14:15:05Z
- [x] T-102 [P0] Release truth and gate hardening: root typecheck covers server, UI and Electron; package, lock, README, CHANGELOG and artifact versions stay one transactional value | verify: release consistency fixtures, rollback injection, exact Electron pin, workflow metadata checks and root typecheck pass at version 0.0.3 | owner: opencode | claim_time: 2026-08-11T14:14:17Z
















## BLOCKED
- [ ] T-104 [P0] Fix release workflow dependency order: install before dependency-backed bumpVersion | verify: release workflow order test passes and GitHub Release Binaries run succeeds | owner: opencode | claim_time: 2026-08-11T16:28:41Z | blocker: GitHub run 31516153390 passed prepare and all Electron builds; WINGET_GITHUB_TOKEN is empty and npm token lacks @saiwork/saiwork publish access
